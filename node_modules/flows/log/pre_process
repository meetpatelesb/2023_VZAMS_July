#!/usr/bin/env bash

#FILES=`find -type f | cut -d/ -f2- | sort -n --reverse | head -n 15`
#echo $FILES
#
#cat $FILES | sort > ../sorted/0.log

while read line
do
  echo $line >> ../pp/raw/${line:0:6}.log
done



# From the run folder, do:
#
# cat `find -type f` | ~/dev/dev/sgprod/dagwood/zz_logistics/metrics/log/pre_process
# cd ../pp/raw
# for f in `find -type f`; do cat $f | sort > ../sorted/$f; done;

